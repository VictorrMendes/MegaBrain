name: n8n.calendar.create_event
abstract_intent: CreateCalendarEvent
description: Autogenerated capability for calendar.create_event
driver: rest_n8n
endpoint: /webhook/calendar/create_event
method: POST
schema:
  type: object
  properties:
    title:
      type: string
    start_time:
      type: string
      description: "The start time of the event in ISO-8601 format (e.g. 2026-07-03T14:00:00Z). Calculate relative dates like 'tomorrow' or 'next friday' based on current context."
    end_time:
      type: string
      description: "The end time of the event in ISO-8601 format. If not specified by user, default to 1 hour after start_time."
