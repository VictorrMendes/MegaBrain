name: n8n.productivity.create_task
abstract_intent: CreateTask
description: Autogenerated capability for productivity.create_task
driver: rest_n8n
endpoint: /webhook/productivity/create_task
method: POST
schema:
  type: object
  properties:
    provider:
      type: string
      description: "MANDATORY: You must extract the provider from the user's text. If the text says 'todoist' OR mentions 'tarefa', value is 'todoist'. If the text says 'notion' OR mentions 'anotação' or 'nota', value is 'notion'."
      enum: [todoist, notion]
    title:
      type: string
      description: The title or content of the task.
    description:
      type: string
      description: Additional details or description for the task.
  required: [provider, title]
